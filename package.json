{
  "name": "slide-editor",
  "version": "0.1.0",
  "description": "HTML slide editor and player with first-class video support",
  "main": "out/main/index.js",
  "type": "module",
  "bin": {
    "slide-agent": "bin/slide-agent"
  },
  "license": "MIT",
  "scripts": {
    "dev": "electron-vite dev",
    "predev": "npm run build:collab",
    "precollab": "npm run build:collab",
    "build": "tsc --noEmit && electron-vite build && npm run build:export && npm run build:collab",
    "build:export": "vite build --config vite.export.config.ts",
    "build:importer": "python3 -m venv .venv-import && ./.venv-import/bin/pip install -q keynote-parser pyinstaller && ./.venv-import/bin/pyinstaller --onefile --name keynote-import --collect-all keynote_parser --collect-all snappy --distpath build/importers --workpath build/pyinstaller --specpath build/pyinstaller importers/keynote/import_keynote.py",
    "setup:importer": "python3 -m venv .venv-import && ./.venv-import/bin/pip install -q keynote-parser",
    "dist": "npm run build && npm run build:importer && electron-builder",
    "dist:linux": "npm run build && npm run build:importer && electron-builder --linux",
    "start": "electron-vite preview",
    "pretest": "node scripts/check-test-environment.cjs",
    "test": "npm run test:serial && npm run test:ordinary",
    "test:ordinary": "vitest run",
    "test:serial": "vitest run --config vitest.serial.config.ts",
    "pretest:long": "node scripts/check-test-environment.cjs",
    "test:long": "vitest run --config vitest.long.config.ts",
    "pretest:performance": "node scripts/check-test-environment.cjs",
    "test:performance": "RUN_PERFORMANCE_STRESS=1 vitest run --config vitest.long.config.ts test/performanceStressBrowser.test.ts",
    "test:formatting:exhaustive": "RUN_EXHAUSTIVE_FORMAT_FUZZ=1 vitest run test/textFormattingExhaustiveBrowser.test.ts",
    "test:interactions:exhaustive": "RUN_EXHAUSTIVE_TEXT_BOX_FUZZ=1 vitest run test/textBoxInteractionExhaustiveBrowser.test.ts",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "export": "npm run build:export >/dev/null && vite-node --config vitest.config.ts scripts/export-deck.mts --",
    "agent": "vite-node --config vitest.config.ts scripts/slide-agent.mts --",
    "collab": "vite-node --config vitest.config.ts scripts/collab-server.mts --",
    "build:collab": "vite build --config vite.collab.config.ts",
    "build:reference": "vite-node --config vitest.config.ts scripts/build-agent-reference.mts",
    "eval:agent": "npm run build:collab && vite-node --config vitest.config.ts scripts/eval-agent.mts",
    "eval:agent:serve": "npm run build:collab && vite-node --config vitest.config.ts scripts/serve-agent-benchmark.mts",
    "eval:papers:serve": "npm run build:collab && vite-node --config vitest.config.ts scripts/serve-paper-benchmark.mts",
    "eval:edits:serve": "npm run build:collab && vite-node --config vitest.config.ts scripts/serve-edit-benchmark.mts",
    "eval:importer": "npm run build:export && vite-node --config vitest.config.ts scripts/eval-importer-regressions.mts",
    "build:katex": "vite-node --config vitest.config.ts scripts/build-katex-inline.mts"
  },
  "build": {
    "appId": "com.scenerepresentations.slide-editor",
    "productName": "DeckWerk",
    "files": [
      "out/**/*",
      "dist/collab/**/*",
      "package.json"
    ],
    "extraResources": [
      {
        "from": "build/importers",
        "to": "importers"
      }
    ],
    "beforePack": "scripts/assert-importer-fresh.cjs",
    "asarUnpack": [
      "**/node_modules/ffmpeg-static/**",
      "**/node_modules/ffprobe-static/**"
    ],
    "mac": {
      "category": "public.app-category.productivity",
      "icon": "resources/deckwerk-icon.icns",
      "target": [
        "dmg",
        "zip"
      ]
    },
    "linux": {
      "category": "Office",
      "target": [
        "AppImage",
        "deb"
      ]
    }
  },
  "dependencies": {
    "@codemirror/lang-css": "^6.3.1",
    "@codemirror/theme-one-dark": "^6.1.3",
    "@types/ws": "^8.18.1",
    "codemirror": "^6.0.2",
    "ffmpeg-static": "^5.2.0",
    "ffprobe-static": "^3.1.0",
    "katex": "^0.18.4",
    "ws": "^8.21.3",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/jsdom": "^30.0.0",
    "@types/node": "^22.7.4",
    "electron": "^32.1.2",
    "electron-builder": "^25.1.8",
    "electron-vite": "^2.3.0",
    "jsdom": "^25.0.1",
    "typescript": "^5.6.2",
    "vite": "^5.4.8",
    "vite-node": "^2.1.1",
    "vitest": "^2.1.1"
  }
}
